/**
* This is a placeholder class to help with imports.
* The actual Vehicle class is in the model package.
*/
public class Vehicle extends model.Vehicle {
    public Vehicle(String vehicleId, String make, String model, int year, String licensePlate,
                  VehicleType type, int capacity, FuelType fuelType) {
        super(vehicleId, make, model, year, licensePlate, type, capacity, fuelType);
    }
 }
 
 