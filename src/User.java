/**
* This is a placeholder class to help with imports.
* The actual User class is in the model package.
*/
public class User extends model.User {
    public User(String userId, String username, String password, String fullName, 
                String email, String phoneNumber, UserRole role) {
        super(userId, username, password, fullName, email, phoneNumber, role);
    }
 }
 
 